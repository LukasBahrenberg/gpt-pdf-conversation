# gpt-pdf-conversation

This is a test repo in Python for using GPT models to generate a conversation about a PDF that is stored and accessed via a vector database.

This repo uses the langchain framework and pinecone as a vector database.